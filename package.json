{
  "name": "node_redis_counter",
  "version": "1.0.0",
  "description": "Nearly pointless nodejs app that endlessly counts a shared counter in redis",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "redis": "^2.6.2"
  }
}
